# MySQL connection
spring.datasource.url=jdbc:mysql://hopper.proxy.rlwy.net:17085/boat_safari_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=UYzvPPuQlPgmAHnPwczjIMfuCpQlkrcr
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optional: Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=8080
# CORS configuration (adjust ports as needed for your frontend)
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000

# Logging
logging.level.com.boatsafari=DEBUG

file.upload-dir=uploads

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Disable caching for development
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false

# Jackson configuration for Hibernate
spring.jackson.serialization.fail-on-empty-beans=false



